# Dependencies — reinstalled inside Docker
node_modules

# Compiled output — rebuilt inside Docker
dist

# Local environment — secrets are injected by Coolify, never baked into the image
.env
.env.*
!.env.example

# Dev tooling
.git
.gitignore
.eslintrc*
.prettierrc*
coverage
test

# Editor / OS noise
.vscode
.idea
*.DS_Store
Thumbs.db

# Docker files themselves (avoid recursive copy)
Dockerfile
.dockerignore
docker-compose*.yml

# Docs
docs
*.md
